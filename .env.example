# 로컬 개발 서버(Vite)가 /auth, /todos 등을 프록시할 백엔드 주소.
# 이 파일을 .env 로 복사한 뒤 값을 채우기 (.env 는 git 에 안 올라감)
VITE_PROXY_TARGET=http://13.125.246.1:8080

# (선택) 프록시 없이 axios 가 직접 이 주소로 요청하게 하고 싶을 때만 설정.
# 비워두면 같은 출처로 요청 → 위 프록시(로컬) 또는 vercel.json 리라이트(배포)가 처리한다.
# VITE_API_BASE_URL=
